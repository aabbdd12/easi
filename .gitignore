# Stata / LaTeX build artefacts and scratch output
*.log
!tests/out/*.log
!replication/R_reference/*.log
docs/*.aux
docs/*.out
docs/*.toc
docs/*.synctex.gz
examples/tempa.dta
examples/tempclus.dta
examples/myfile.xml
examples/*.gph
docs/fig/*.gph

# the WELCOM toolkit is a separate product; easi is published on its own
Installer/
# the R 3.4.4 installer and the archived R package binary (82 MB); see
# tests/R_reference/check_binary_r34.R for where they come from
easi/
# the original WELCOM copies of the wrapper; the shipped one is src/sr_easi.ado
/sr_easi.ado
/sr_easi.dlg
/sr_easi.hlp
# the note is shipped as a PDF only; its sources, tables and figures stay local
docs/archive/
docs/audit_R_vs_easi.md
docs/*.tex
docs/fig/
docs/*.png
examples/*.png
examples/*.duvm
examples/hixdata_levels.dta

# the test suite stays local; replication/ carries it under descriptive
# names, together with the frozen R reference.  Its outputs are regenerated.
tests/
replication/out/
